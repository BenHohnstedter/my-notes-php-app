version: "3.2"
services:
  web:
    build: .
    environment:
      VIRTUAL_HOST: my-note-opp.php-app.dev,my-note-opp.bhohnstedter-nb.php-app
    volumes:
      - .:/var/www/html/

networks:
  default:
    name: docker_default
    external: true
